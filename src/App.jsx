import { Button } from 'react-bootstrap';

function App() {
    return (
        <div>
            <Button className='custom-class'>React Bootstrap Button</Button>
        </div>
    );
}

export default App;
