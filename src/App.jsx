import { Button } from 'react-bootstrap';

function App() {
    return (
        <div>
            <Button className='custom-class'>React Bootstrap Button sds</Button>
            <Button className='custom-class'>React Bootstrap Button sds</Button>
        </div>
    );
}

export default App;
